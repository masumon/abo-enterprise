services:
  # ─── API Gateway ───────────────────────────
  api-gateway:
    build:
      context: .
      dockerfile: services/api_gateway/Dockerfile
    ports:
      - "${API_GATEWAY_PORT:-8000}:8000"
    environment:
      - APP_ENV=${APP_ENV:-development}
      - POSTGRES_HOST=postgres
      - REDIS_HOST=redis
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aegis-net

  # ─── Supervisor Service ────────────────────
  supervisor:
    build:
      context: .
      dockerfile: services/supervisor/Dockerfile
    environment:
      - APP_ENV=${APP_ENV:-development}
      - REDIS_HOST=redis
      - POSTGRES_HOST=postgres
    depends_on:
      - redis
      - postgres
    networks:
      - aegis-net

  # ─── Agent Service ─────────────────────────
  agents:
    build:
      context: .
      dockerfile: services/agents/Dockerfile
    environment:
      - APP_ENV=${APP_ENV:-development}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GOOGLE_AI_API_KEY=${GOOGLE_AI_API_KEY}
      - QDRANT_HOST=qdrant
      - REDIS_HOST=redis
    depends_on:
      - redis
      - qdrant
    networks:
      - aegis-net

  # ─── Execution Service ─────────────────────
  execution:
    build:
      context: .
      dockerfile: services/execution/Dockerfile
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - APP_ENV=${APP_ENV:-development}
      - REDIS_HOST=redis
    depends_on:
      - redis
    networks:
      - aegis-net

  # ─── Approval Service ─────────────────────
  approval:
    build:
      context: .
      dockerfile: services/approval/Dockerfile
    environment:
      - APP_ENV=${APP_ENV:-development}
      - POSTGRES_HOST=postgres
      - REDIS_HOST=redis
    depends_on:
      - postgres
      - redis
    networks:
      - aegis-net

  # ─── Auth Service ──────────────────────────
  auth:
    build:
      context: .
      dockerfile: services/auth/Dockerfile
    environment:
      - APP_ENV=${APP_ENV:-development}
      - POSTGRES_HOST=postgres
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - OAUTH_GITHUB_CLIENT_ID=${OAUTH_GITHUB_CLIENT_ID}
      - OAUTH_GITHUB_CLIENT_SECRET=${OAUTH_GITHUB_CLIENT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - aegis-net

  # ─── Analytics Service ─────────────────────
  analytics:
    build:
      context: .
      dockerfile: services/analytics/Dockerfile
    environment:
      - APP_ENV=${APP_ENV:-development}
      - POSTGRES_HOST=postgres
      - MONGO_HOST=mongodb
    depends_on:
      - postgres
      - mongodb
    networks:
      - aegis-net

  # ─── Frontend Dashboard ────────────────────
  dashboard:
    build:
      context: ./frontend/dashboard
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:${API_GATEWAY_PORT:-8000}
    networks:
      - aegis-net

  # ─── Infrastructure: PostgreSQL ────────────
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-aegis_ai}
      POSTGRES_USER: ${POSTGRES_USER:-aegis}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aegis_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aegis}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - aegis-net

  # ─── Infrastructure: MongoDB ───────────────
  mongodb:
    image: mongo:7
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-aegis}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-aegis_dev}
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - aegis-net

  # ─── Infrastructure: Redis ─────────────────
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD:-aegis_dev}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-aegis_dev}", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - redis_data:/data
    networks:
      - aegis-net

  # ─── Infrastructure: Qdrant ────────────────
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "${QDRANT_PORT:-6333}:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - aegis-net

  # ─── Monitoring: Prometheus ────────────────
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./infrastructure/kubernetes/configmaps/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    networks:
      - aegis-net

  # ─── Monitoring: Grafana ───────────────────
  grafana:
    image: grafana/grafana:latest
    ports:
      - "${GRAFANA_PORT:-3001}:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - aegis-net

volumes:
  postgres_data:
  mongo_data:
  redis_data:
  qdrant_data:
  prometheus_data:
  grafana_data:

networks:
  aegis-net:
    driver: bridge

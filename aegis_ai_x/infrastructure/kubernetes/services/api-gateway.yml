apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: aegis-ai
  labels:
    app: aegis-ai
    component: api-gateway
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: aegis-ai
    component: api-gateway

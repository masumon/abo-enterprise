apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: sumonix-ai
  labels:
    app: sumonix-ai
    component: api-gateway
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: sumonix-ai
    component: api-gateway

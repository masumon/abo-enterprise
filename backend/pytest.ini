[pytest]
# Restrict collection to tests/ — without this, pytest's default *_test.py
# discovery pattern also matches production route files like
# app/api/v1/routes/steadfast_test.py (named for the "Steadfast Test
# Connection" admin feature, not because it's a test module) and tries to
# run their real functions as test cases.
testpaths = tests
